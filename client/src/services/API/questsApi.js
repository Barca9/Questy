/*
import createInstance from "../axiosInstance";

const API = (config = {}) => createInstance(config);

export const getAllQuests = async (url = baseUrl) => {
    const response = await API({ baseUrl: url}).get()

    return response
}*/
